# Repository Rules

## Version Control & Git Pushes
- **CRITICAL:** Do NOT run `git push` or push modifications to remote branches (GitHub/GitLab) unless the user explicitly asks you to do so or approves it first in the chat conversation.
